// 
/**
 * Runtime Config
 * web app 运行配置
 * {@link https://umijs.org/docs/runtime-config}
 * {@name modifyClientRenderOpts(fn) }
 * {@name patchRoutes({ routes }) }
 * {@name render(oldRender: Function) }
 * {@name onRouteChange({ routes, matchedRoutes, location, action }) }
 * {@name rootContainer(LastRootContainer) }
 */

export function patchRoutes({ routes }) {
  console.log(routes)
}